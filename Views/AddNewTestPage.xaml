<Page x:Class="TestingService.Views.AddNewTestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestingService.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" Background="White"
      Title="Создание теста">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="30,0,0,0">
            <Button Content="Сохранить тест" Width="150" Height="30" Margin="10,0,0,0"/>
            <Button Content="Добавить вопрос" Command="{Binding AddNewQuestion}" Width="150" Height="30" Margin="50,0,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Margin="30,0,0,0" HorizontalAlignment="Left">
            <TextBlock Text="Название теста*"/>
            <TextBox Width="500" TextWrapping="Wrap"/>
            <TextBlock Text="Описание теста" Margin="0,10,0,0"/>
            <TextBox Width="500" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Page>
