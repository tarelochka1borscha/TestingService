<Window x:Class="TestingService.Views.AddNewTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestingService.Views"
        mc:Ignorable="d"
        Title="Создание теста" Height="800" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.05*"/>
            <RowDefinition Height="0.1*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="30,0,0,0">
            <Button Content="На главную" Width="100" Height="30"/>
            <Button Content="Сохранить тест" Width="150" Height="30" Margin="10,0,0,0"/>
            <Button Content="Добавить вопрос" Width="150" Height="30" Margin="50,0,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Margin="30,0,0,0" HorizontalAlignment="Left">
            <TextBlock Text="Название теста*"/>
            <TextBox Width="500"/>
            <TextBlock Text="Описание теста" Margin="0,10,0,0"/>
            <TextBox Width="500"/>
        </StackPanel>
    </Grid>
</Window>
