<UserControl x:Class="TestingService.Views.QuestionViews.QuestionEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestingService.Views.QuestionViews"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="Заголовок вопроса*"/>
            <TextBox Width="300" TextWrapping="Wrap" Margin="0,5,0,10"/>
            <TextBlock Text="Описание"/>
            <TextBox Width="300" TextWrapping="Wrap" Margin="0,5,0,10"/>
            <TextBlock Text="Тип вопроса*"/>
            <ComboBox Width="300" Margin="0,5,0,10"/>
            <Button Command="{Binding AddNewAnswer}" Content="Добавить ответ"/>
        </StackPanel>
    </Grid>
</UserControl>
